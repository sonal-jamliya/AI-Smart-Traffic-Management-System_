#include<stdio.h>
void main()
{
	int num ,sum=0,i=1;
	printf("enter the number");
	scanf("%d",&num);
	
	while(i<num)
	{
		if(num=%i==0)
		sum=sum+i;
		i++;
	}
	if(sum==num)
	printf("%d is a perfect number",num);
	else
	printf("%d is not a perfect number",num);
}
